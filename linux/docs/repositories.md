# Linux repositories

Document stable repository roots, ownership, writable paths, default branches, and routing hints for the deployed Linux worker. Runtime state belongs in the Node Registry; credentials and private tokens must not be stored here.
