# Node documentation

Keep one short Markdown file per configured remote node. Describe stable facts only: architecture, intended repositories/resources, important tools, restrictions and any routing hints. Runtime load/online state belongs to Node Registry, not these files.
